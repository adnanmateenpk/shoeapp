<%= link_to("Back to Main",{:action=>'index'},:class=>'action index')%>
<%= form_tag({:action=>"send_mail"})%>
<table>
<tr>
  <th>Subject</th>
  <td><%= text_field_tag("Subject")%></td>
</tr>
<tr>
  <th>Write a message</th>
  <td><%= text_area_tag("message", nil, rows:10, cols:25)%></td>
</tr>
</table>
<%= submit_tag("Send email")%>
